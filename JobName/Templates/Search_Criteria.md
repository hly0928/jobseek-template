# Search Criteria

## Sources and queries

- Sources: `{{SOURCES}}`
- Queries: `{{QUERIES}}`
- Location and radius: `{{LOCATION_FILTERS}}`
- Recency: `{{RECENCY}}`

## Assessment policy

The only executable hard-exclusion definitions, score components and eligibility
threshold are in `.jobseek/config.json` under this track's `assessment` object.
This file defines sources, queries, search intent and preferences only.
